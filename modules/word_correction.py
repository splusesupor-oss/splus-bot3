import random
from itertools import count

# Every left side is intentionally misspelled; every right side is the correct form.
WORDS = [
('مسولیت', 'مسئولیت'),('مسول', 'مسئول'),('مسله', 'مسئله'),('مسایل', 'مسائل'),('تاثیر', 'تأثیر'),('تاثیرات', 'تأثیرات'),('تائید', 'تأیید'),('تاید', 'تأیید'),('تامین', 'تأمین'),('تالیف', 'تألیف'),
('تالیفات', 'تألیفات'),('تاسیس', 'تأسیس'),('موثر', 'مؤثر'),('موثرترین', 'مؤثرترین'),('مومن', 'مؤمن'),('مالف', 'مؤلف'),('متاسفانه', 'متأسفانه'),('متاسفم', 'متأسفم'),('مطمین', 'مطمئن'),('مطمینم', 'مطمئنم'),
('مبهمم', 'مبهم'),('اساد', 'اساتید'),('مهمم', 'مهم'),('معنائی', 'معنایی'),('ظبط', 'ضبط'),('ظبطی', 'ضبطی'),('ظمانت', 'ضمانت'),('ظابطه', 'ضابطه'),('اظطراب', 'اضطراب'),('اظطرابات', 'اضطرابات'),
('تحقیقان', 'تحقیقات'),('قظع', 'قطع'),('قظعی', 'قطعی'),('قظعا', 'قطعاً'),('ظمینه', 'زمینه'),('کظور', 'کسور'),('لظت', 'لذت'),('حافظطه', 'حافظه'),('مشخصطه', 'مشخصه'),('محیطط', 'محیط'),
('مساحتط', 'مساحت'),('صفحه\u200cه', 'صفحه'),('حسابه', 'حساب'),('محسابه', 'محاسبه'),('ملاحضه', 'ملاحظه'),('ملاحضات', 'ملاحظات'),('زوح', 'روح'),('اهّم', 'اهم'),('وحشتط', 'وحشت'),('هیجانط', 'هیجان'),
('تفاهمط', 'تفاهم'),('سطحح', 'سطح'),('تسلیحاتط', 'تسلیحات'),('تحصیلاتط', 'تحصیلات'),('مشاهدطه', 'مشاهده'),('خصوصیتط', 'خصوصیت'),('تجربه\u200cه', 'تجربه'),('کتابه', 'کتاب'),('مدرسه\u200cه', 'مدرسه'),('خانه\u200cه', 'خانه'),
('مهمون', 'مهمان'),('مهمونی', 'مهمانی'),('علاقه\u200cه', 'علاقه'),('بخصوص', 'به\u200cخصوص'),('مخصوصن', 'مخصوصاً'),('اختصاصیی', 'اختصاصی'),('وضعییت', 'وضعیت'),('نقشطه', 'نقطه'),('مقداره', 'مقدار'),('تعدادیی', 'تعدادی'),
('بسیاریی', 'بسیاری'),('مهمونیها', 'مهمانی\u200cها'),('میباشد', 'می\u200cباشد'),('میخواهم', 'می\u200cخواهم'),('میخواهد', 'می\u200cخواهد'),('نمیدانم', 'نمی\u200cدانم'),('نمی توانم', 'نمی\u200cتوانم'),('نمیخواهم', 'نمی\u200cخواهم'),('نمیفهمم', 'نمی\u200cفهمم'),('نمیخواد', 'نمی\u200cخواهد'),
('میخوای', 'می\u200cخواهی'),('میرم', 'می\u200cروم'),('هم اکنون', 'هم\u200cاکنون'),('برنامه ریزی', 'برنامه\u200cریزی'),('پیش بینی', 'پیش\u200cبینی'),('دانش اموز', 'دانش\u200cآموز'),('خانه داری', 'خانه\u200cداری'),('همدیگه', 'همدیگر'),('بخاطر', 'به خاطر'),('بیاد', 'بیاید'),
('بیار', 'بیاور'),('میگن', 'می\u200cگویند'),('میگفت', 'می\u200cگفت'),('میخونم', 'می\u200cخوانم'),('استفاده کردن', 'استفاده\u200cکردن'),('برخواستن', 'برخاستن'),('میتونی', 'می\u200cتوانی'),('میتونم', 'می\u200cتوانم'),('میتونست', 'می\u200cتوانست'),('میخواست', 'می\u200cخواست'),
('علاقمندی', 'علاقه\u200cمندی'),('علاقمند', 'علاقه\u200cمند'),('دلبستگیی', 'دلبستگی'),('وابستگیی', 'وابستگی'),('موضوعیی', 'موضوعی'),('خصوصیی', 'خصوصی'),('توانائی', 'توانایی'),('زروری', 'ضروری'),('زعیف', 'ضعیف'),('قانونیی', 'قانونی'),
('فرهنگیی', 'فرهنگی'),('موافع', 'موافق'),('مخالفط', 'مخالفت'),('مربوطه', 'مربوط'),('برعکسس', 'برعکس'),('پرفسور', 'پروفسور'),('تکنولژی', 'تکنولوژی'),('تجزیح', 'تجزیه'),('توصیح', 'توصیه'),('توجیح', 'توجیه'),
]
_active = {}
_remaining = {}
_FALLBACK_TOKENS = count(1)


def _next_token():
    """توکن یکتا که با ری‌استارت ربات تکرار نمی‌شود.

    شمارندهٔ حافظه‌ای با هر ری‌استارت از ۱ شروع می‌شد و مرجع جایزه را
    تکراری می‌کرد، پس دفتر تراکنش پرداخت را رد می‌کرد و کاربر سکه‌ای
    نمی‌گرفت. حالا از شمارندهٔ ماندگار اقتصاد استفاده می‌شود.
    """
    try:
        from economy import rewards as _rewards
        return _rewards.round_id()
    except Exception:
        # اگر اقتصاد در دسترس نبود، بازی نباید بخوابد.
        return next(_FALLBACK_TOKENS)


def start(chat_id):
    remaining = _remaining.get(chat_id)
    if not remaining:
        remaining = list(range(len(WORDS)))
        _remaining[chat_id] = remaining
    index = random.choice(remaining)
    remaining.remove(index)
    wrong, correct = WORDS[index]
    token = _next_token()
    _active[chat_id] = {'wrong': wrong, 'correct': correct, 'token': token}
    return dict(_active[chat_id])

def _letters(value):
    """فقط حروف: فاصله و نیم‌فاصله نادیده گرفته می‌شوند."""
    return (
        str(value or "").strip()
        .replace("\u200c", "").replace("\u200f", "").replace("\u200e", "")
        .replace("ي", "ی").replace("ك", "ک")
        .replace(" ", "")
    )


def _spacing(value):
    """حروف به‌همراه فاصله‌گذاری، با تفکیک نیم‌فاصله از فاصلهٔ معمولی.

    برای جفت‌هایی مثل «هم اکنون» → «هم‌اکنون» تفاوت *فقط* در همین است.
    اگر نیم‌فاصله را با فاصله یکی بگیریم، شکل غلط هم درست شمرده می‌شود و
    بازی غیرقابل‌باخت می‌گردد. پس نیم‌فاصله به نشانهٔ اختصاصی نگاشت
    می‌شود و فقط فاصله‌های تکراری فشرده می‌گردند.
    """
    text = (
        str(value or "").strip()
        .replace("\u200f", "").replace("\u200e", "").replace("\ufeff", "")
        .replace("ي", "ی").replace("ك", "ک")
    )
    text = " ".join(text.split(" "))
    while "  " in text:
        text = text.replace("  ", " ")
    return text.replace("\u200c", "\x00").strip()


def _norm(value):
    """سازگاری با کد قدیمی."""
    return _letters(value)


def answer(chat_id, text):
    """پاسخ را بررسی می‌کند.

    ``None`` یعنی «این پیام به بازی تصحیح کلمات ربطی ندارد» و هندلر باید
    به بازی‌های بعدی اجازهٔ بررسی بدهد. پیش از این هر متنی ``False``
    برمی‌گرداند و چون هندلر روی مقدار غیر-None زودتر return می‌کرد، بازیِ
    فعالِ تصحیح کلمات جلوی «چیستان»، «جای خالی» و بقیه را می‌گرفت.
    """
    data = _active.get(chat_id)
    if not data:
        return None
    if not _letters(text):
        return None

    correct, wrong = data['correct'], data['wrong']
    # وقتی تفاوت غلط و درست فقط در فاصله/نیم‌فاصله است، فاصله‌گذاری بخشی از
    # پاسخ است و نباید نادیده گرفته شود؛ در غیر این صورت آن جفت اصلاً قابل
    # حل نمی‌بود. برای بقیه، فاصله‌گذاری سخت‌گیرانه نیست.
    spacing_matters = _letters(correct) == _letters(wrong)
    if spacing_matters:
        if _spacing(text) == _spacing(correct):
            _active.pop(chat_id, None)
            return True
        if _spacing(text) == _spacing(wrong):
            return False
        return None

    if _letters(text) == _letters(correct):
        _active.pop(chat_id, None)
        return True
    # فقط وقتی «پاسخ غلط» اعلام می‌شود که کاربر واقعاً روی همین کلمه تلاش
    # کرده باشد؛ پیام‌های نامرتبط اصلاً مصرف نمی‌شوند.
    if _letters(text) == _letters(wrong):
        return False
    return None

def get(chat_id):
    return dict(_active[chat_id]) if chat_id in _active else None

def clear(chat_id, token=None):
    data = _active.get(chat_id)
    if data and (token is None or data['token'] == token):
        _active.pop(chat_id, None)
        return True
    return False
